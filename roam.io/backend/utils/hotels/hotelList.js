const axios = require('axios')

// This middleware function can be used to retrieve the hotels around a given zipCode or location.
export async function getHotelList({ amadeusToken, zipCode, location }) {

}